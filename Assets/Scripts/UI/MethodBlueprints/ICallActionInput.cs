using System;

namespace UI.MethodBlueprints
{
    public interface ICallActionInput : IMethodBluePrintInput<Action>
    {
        // public void RegisterMethodToCall(Action handler, IMethodProvider methodProvider);
    }
}